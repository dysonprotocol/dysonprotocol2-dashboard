{
    "request": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "$ref": "#/definitions/dysonprotocol.crontask.v1.MsgCreateTask",
        "definitions": {
            "dysonprotocol.crontask.v1.MsgCreateTask": {
                "properties": {
                    "creator": {
                        "type": "string",
                        "description": "Account creating the scheduled task."
                    },
                    "scheduled_timestamp": {
                        "type": "string",
                        "description": "Unix timestamp or duration offset when the task is scheduled to execute. Can be either a Unix timestamp or a time offset prefixed with \"+\" (e.g. \"+1h30m\"). Offset is relative to the current block time."
                    },
                    "expiry_timestamp": {
                        "type": "string",
                        "description": "Unix timestamp or duration offset after which the task will expire if not executed. Can be either a Unix timestamp or a time offset prefixed with \"+\" (e.g. \"+2h\"). When using an offset, it's relative to the scheduled_timestamp. If empty, defaults to scheduled_timestamp + ExpiryLimit."
                    },
                    "task_gas_limit": {
                        "type": "string",
                        "description": "Maximum gas limit for the task execution."
                    },
                    "task_gas_fee": {
                        "$ref": "#/definitions/cosmos.base.v1beta1.Coin",
                        "additionalProperties": false,
                        "description": "Gas fee for the task execution, deducted at execution time."
                    },
                    "msgs": {
                        "items": {
                            "properties": {
                                "type_url": {
                                    "type": "string",
                                    "description": "The type url prefixed with a slash, like '/cosmos.bank.v1beta1.MsgSend'"
                                },
                                "value": {
                                    "type": "string",
                                    "description": "Must be a valid serialized protocol buffer of the above specified type.",
                                    "format": "binary",
                                    "binaryEncoding": "base64"
                                }
                            },
                            "additionalProperties": false,
                            "type": "object",
                            "title": "Any",
                            "description": "Contains the serialized protocol buffer message using the response bytes from `/dysonprotocol.script.v1.QueryEncodeJsonRequest`"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Messages to execute as part of the task when triggered."
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Msg Create Task",
                "description": "MsgCreateTask creates a new scheduled task with specified execution time and messages. Behavior: - Creates a task scheduled for execution at a future timestamp. - Parses flexible timestamp formats (Unix timestamps or duration offsets like \"+1h30m\"). - Validates scheduling constraints and gas limits against module parameters. - Calculates gas price from fee and limit, stores task with unpacked messages. - Tasks remain in SCHEDULED status until execution time. Validation: - Creator address must be valid. - Scheduled timestamp must be in the future and within MaxScheduledTime limit. - Expiry timestamp must be after scheduled time (defaults to scheduled + ExpiryLimit if not provided). - Gas limit must be positive and not exceed BlockGasLimit. - Gas fee must be positive and denominated in \"udys\". - At least one message must be provided in the task. Emits: - EventTaskCreated(task_id, creator) on successful task creation. Returns: - MsgCreateTaskResponse with allocated task ID."
            },
            "cosmos.base.v1beta1.Coin": {
                "properties": {
                    "denom": {
                        "type": "string"
                    },
                    "amount": {
                        "type": "string"
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Coin",
                "description": "Coin defines a token with a denomination and an amount. NOTE: The amount field is an Int which implements the custom method signatures required by gogoproto."
            }
        }
    },
    "response": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "$ref": "#/definitions/dysonprotocol.crontask.v1.MsgCreateTaskResponse",
        "definitions": {
            "dysonprotocol.crontask.v1.MsgCreateTaskResponse": {
                "properties": {
                    "task_id": {
                        "type": "string",
                        "description": "Unique identifier assigned to the newly created task."
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Msg Create Task Response",
                "description": "MsgCreateTaskResponse contains the result of task creation. Empty response indicates successful task creation with allocated task ID."
            }
        }
    }
}