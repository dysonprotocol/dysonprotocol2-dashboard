{
    "request": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "$ref": "#/definitions/dysonprotocol.whaleswap.v1.MsgCreatePool",
        "definitions": {
            "dysonprotocol.whaleswap.v1.MsgCreatePool": {
                "properties": {
                    "creator": {
                        "type": "string",
                        "description": "Account creating the pool; receives the initial minted shares."
                    },
                    "coins": {
                        "items": {
                            "$ref": "#/definitions/cosmos.base.v1beta1.Coin"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Initial reserves. Required: exactly two coins with positive amounts. Order is canonicalized internally to pool denom order (lexicographic)."
                    },
                    "min_initial_collateral_ratio": {
                        "items": {
                            "$ref": "#/definitions/cosmos.base.v1beta1.DecCoin"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Required: minimum collateral ratio per reserve denom (exactly two, canonical order). Each amount is a LegacyDec string (> 1)."
                    },
                    "interest_rate": {
                        "items": {
                            "$ref": "#/definitions/cosmos.base.v1beta1.DecCoin"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Annual interest rates per reserve denom (APR >= 0). Input may include 0, 1, or 2 entries; keeper normalizes to exactly two entries in canonical pool order."
                    },
                    "max_borrow_percent": {
                        "items": {
                            "$ref": "#/definitions/cosmos.base.v1beta1.DecCoin"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Required: maximum borrow capacity per reserve denom as DecCoins (exactly two, canonical order). Each amount is a LegacyDec in [0,1)."
                    },
                    "liquidation_threshold": {
                        "items": {
                            "$ref": "#/definitions/cosmos.base.v1beta1.DecCoin"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Required: collateral ratio liquidation threshold per reserve denom (exactly two, canonical order; each amount is a LegacyDec string > 1)"
                    },
                    "fee_rate": {
                        "items": {
                            "$ref": "#/definitions/cosmos.base.v1beta1.DecCoin"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Per-denom swap fee rates (amounts in [0,1)). Allow 0, 1, or 2 entries; keeper normalizes to exactly two entries in canonical pool order. Replaces deprecated fee_pct field 5."
                    },
                    "bound_percent": {
                        "items": {
                            "$ref": "#/definitions/cosmos.base.v1beta1.DecCoin"
                        },
                        "additionalProperties": false,
                        "type": "array",
                        "description": "Optional: per-denom directional price-impact bounds keyed by the sold denom. When omitted, defaults to 1 for both pool denoms (unbounded). When provided, must contain exactly two entries in canonical pool order with amounts satisfying 0 < x <= 1; x == 1 disables the bound for that denom."
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Msg Create Pool",
                "description": "MsgCreatePool"
            },
            "cosmos.base.v1beta1.Coin": {
                "properties": {
                    "denom": {
                        "type": "string"
                    },
                    "amount": {
                        "type": "string"
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Coin",
                "description": "Coin defines a token with a denomination and an amount. NOTE: The amount field is an Int which implements the custom method signatures required by gogoproto."
            },
            "cosmos.base.v1beta1.DecCoin": {
                "properties": {
                    "denom": {
                        "type": "string"
                    },
                    "amount": {
                        "type": "string"
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Dec Coin",
                "description": "DecCoin defines a token with a denomination and a decimal amount. NOTE: The amount field is an Dec which implements the custom method signatures required by gogoproto."
            }
        }
    },
    "response": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "$ref": "#/definitions/dysonprotocol.whaleswap.v1.MsgCreatePoolResponse",
        "definitions": {
            "dysonprotocol.whaleswap.v1.MsgCreatePoolResponse": {
                "properties": {
                    "pool_id": {
                        "type": "string",
                        "description": "Newly created pool id."
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Msg Create Pool Response",
                "description": "Empty response. See EventPoolCreated for emitted details."
            }
        }
    }
}