{
    "request": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "$ref": "#/definitions/dysonprotocol.script.v1.WebRequest",
        "definitions": {
            "dysonprotocol.script.v1.WebRequest": {
                "properties": {
                    "script_address": {
                        "type": "string",
                        "description": "Bech32 account address of the script to execute."
                    },
                    "script_name": {
                        "type": "string",
                        "description": "Nameservice name of the script to execute (e.g., \"example.dys\")."
                    },
                    "httprequest": {
                        "type": "string",
                        "description": "HTTP request string to pass to the script's WSGI application."
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Web Request",
                "description": "WebRequest requests execution of a script's WSGI web application. Either script_address or script_name must be provided. The script's WSGI application will be executed in read-only mode with no state modifications allowed."
            }
        }
    },
    "response": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "$ref": "#/definitions/dysonprotocol.script.v1.WebResponse",
        "definitions": {
            "dysonprotocol.script.v1.WebResponse": {
                "properties": {
                    "httpresponse": {
                        "type": "string",
                        "description": "HTTP response string returned by the script's WSGI application."
                    }
                },
                "additionalProperties": false,
                "type": "object",
                "title": "Web Response",
                "description": "WebResponse contains the HTTP response from the script's WSGI application."
            }
        }
    }
}